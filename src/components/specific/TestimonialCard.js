import React from "react";
import styled from "styled-components";
const StyledContainer = styled.div``;
export const TestimonialCard = React.memo(props => {
  return <StyledContainer>TestimonialCard</StyledContainer>;
});
