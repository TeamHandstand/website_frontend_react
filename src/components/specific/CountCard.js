import React from "react";
import styled from "styled-components";
const StyledContainer = styled.div``;
export const CountCard = React.memo(props => {
  return <StyledContainer>CountCard</StyledContainer>;
});
